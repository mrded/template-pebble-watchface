name: Pebble Build

on:
  push:
    branches: ["**"]
    tags: ["v*"]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Check project is initialized
        id: init
        run: |
          if [ -d src ] && [ -f package.json ]; then
            echo "initialized=true" >> "$GITHUB_OUTPUT"
          else
            echo "initialized=false" >> "$GITHUB_OUTPUT"
            echo "No project found — run 'pebble new-project <name>' to initialize."
          fi

      - name: Set version from tag
        if: steps.init.outputs.initialized == 'true' && startsWith(github.ref, 'refs/tags/v')
        run: |
          TAG="${GITHUB_REF#refs/tags/v}"
          MAJOR=$(echo "$TAG" | cut -d. -f1)
          MINOR=$(echo "$TAG" | cut -d. -f2)
          VERSION="${MAJOR}.${MINOR}.0"
          jq --arg v "$VERSION" '.version = $v' package.json > package.json.tmp && mv package.json.tmp package.json
          echo "Version set to $VERSION"

      - name: Set up Docker Buildx
        if: steps.init.outputs.initialized == 'true'
        uses: docker/setup-buildx-action@v3

      - name: Build Pebble Docker image
        if: steps.init.outputs.initialized == 'true'
        run: docker build --platform linux/amd64 -t pebble-builder .

      - name: Run Pebble build
        if: steps.init.outputs.initialized == 'true'
        run: |
          mkdir -p build resources
          docker run --rm --platform linux/amd64 \
            -v "$(pwd)/src:/app/src:ro" \
            -v "$(pwd)/resources:/app/resources:ro" \
            -v "$(pwd)/package.json:/app/package.json:ro" \
            -v "$(pwd)/wscript:/app/wscript:ro" \
            -v "$(pwd)/build:/app/build" \
            pebble-builder

      - name: Upload build artifact
        if: steps.init.outputs.initialized == 'true'
        uses: actions/upload-artifact@v4
        with:
          name: pebble-app
          path: build/*.pbw

      - name: Create Release
        if: steps.init.outputs.initialized == 'true' && startsWith(github.ref, 'refs/tags/v')
        uses: softprops/action-gh-release@v2
        with:
          files: build/*.pbw
          generate_release_notes: true
